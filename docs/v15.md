# /tmp fixes

- apparently OS X is not actually a real operating system for real people to do real things with and /tmp does not actually function correctly
- TOOD work around this

- perhaps we could make a new .gitignore'd .scriptorium/ folder to put worktrees in
- this would also make it more obvious for users where the worktrees are, and easier for them to nuke it if needed.
