# v2 plan

- poking stuck coding agents
  - if they are stuck and have not submit a PR, prod them to continue working.


think we need to be parsing the log output from codex and detect when the agent has stopped working (perhaps it wants to prompt the user for questions, or tests are still failing, or such) and remind the agent to continue working and repeat the original ticket.

- i'm not sure how exactly this should work with codex, we might need to be flexible on implementation.


## TODO list

- [ ] log parsing
  - I think codex supports json output? could use jsony to parse?
  - we can use `codex exec --json` to get a json stream, this is what we want, to watch for when a turn is completed.
  - we also should pass through useful logging on `scriptorium run` like "agent for this ticket is working on this file..."
  - there is an `--output-schema` flag for a final structured output, but this is NOT what we want for our current case. output-schema is only for the final output, not for intermediate steps.
- [ ] stall detection
  - build basic stall detection to poke the agent when a stall is detected (turn ended but no pr submitted)
  - remind the agent what the original ticket is.
- [ ] stall + test failure detection
  - further augment stall detection to also run tests and then prod the agent to continue working on the ticket (remind them the original ticket) and also attach the current test failure output.

- out of scope for this ticket:
  - coding agent promotions - perhaps having managers promote failing code agents? out of scope for this ticket.
