# v5 plan


- multi-ticket parallelism
  - run multiple coding agents concurrently on independent tickets
  - requires v3 observability and v4 code review agent to be in place first


## TODO list

- [ ] parallel ticket assignment
  - assign N tickets concurrently when they touch independent areas/files
  - detect conflicts (overlapping files/areas) and serialize those tickets
- [ ] concurrent agent execution
  - run multiple coding agent processes in parallel
  - configurable concurrency limit (start with 2-3)
- [ ] merge queue ordering
  - handle multiple in-flight merge queue entries
  - serialize merges to master to avoid conflicts
- [ ] resource management
  - monitor API rate limits and token budget across parallel agents
  - backpressure when approaching limits


- out of scope
  - speculative execution (starting tickets that might conflict) - keep it simple, only parallelize clearly independent work
  - dynamic concurrency scaling - start with a fixed limit, tune later with v3 metrics data
