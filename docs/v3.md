# v3 plan


- improve observability of the system
- ticket success tracking
  - what % of tickets succeed on first attempt vs reopened/parked?
  - why do tickets fail? categorize failure modes.
- ticket predictions
  - how hard is the ticket, how long might it take?
- ticket metrics
- ticket post-analysis
  - how hard was it? how long did it take?


## TODO list

- [ ] ticket success tracking
  - track success/reopen/park outcomes per ticket
  - record attempt count, failure reason (stall, test failure, merge conflict, parked)
  - surface first-attempt success rate as a key metric
  - use this data to identify recurring failure patterns and inform prompt improvements
- [ ] metrics collection
  - any metrics we can get. how long do tickets take, how many tokens were used (or characters if we can't get token counts)
- [ ] ticket predictions
  - setup a prompt to predict the difficulty and duration of a ticket.
  - should we use openai_leap or codex?
    - calling out to openai_leap would be simple and efficient but then we would add complexity on picking models and API keys.
    - perhaps for now we should focus on just using codex.
- [ ] post-analysis
  - after a ticket is done, analyze the ticket and provide a post-analysis.
  - how hard was it? how long did it take?


- out of scope
  - detailed post-analysis - this could be a very long (and useful) rabbit hole, but we should keep it simple for now as a first pass.
  - dynamic routing on difficulty - this would be difficult to implement properly. we should focus on metrics for now and dialing things in.
    - our 'predictor' agent should get the prompt improved to be more accurate before we think about dynamic routing.
