# v11

- dynamic model selection
- we should add a way to dynamically pick a model to use for the coder based on the predicted difficulty of the task.
- probably just support up to 3 models to pick from. one slow/smart (opus class), one medium (sonnet class), and one small (hauku range, qwen3 4b / 9b range)
- we will also need some cleverness to also handle promoting models if the small fast models fail to properly sove a ticket.
