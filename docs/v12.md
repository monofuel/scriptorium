# v12 plan

code quality pass

- monofuel note: spending some time reviewing the code of a fresh scriptorium built project, it mostly looks good but I have a bunch of nitpicks.
- we should improve src/scriptorium/prompts/ for the coding agent

## coding nitpicks

- the coding agent keeps doing 'python style imports" where all the imports are on individual import lines. this is not right.
  - nim style imports use a single import block, and we personally want them sorted.

bad import example:

```
import unittest
import "../src/game"
import "../src/systems/input"
import "../src/entities/character"
```

good import example:

```
import
  std/[unittest, strformat, strutils],
  boxy, silky,
  game, input, character,
```
import order should be "std/[] imports first, then libraries, then local imports.

- next related nit, we should have a tests/config.nims with `--path:"../src"` as the contents.
  - this enables tests to automatically import src/ files without having to use an absolute ../src/ path.


- next, commenting standards.
  - all (or maybe most) functions should have a nimdoc comment.
  - nimdoc comments start INSIDE the proc block, and start with ##

- also, this is more pedantic, but I would like to ensure that ALL comments are complete sentences with a capital first letter and the comment ends with a period.
  - this makes comments good to read in nimdocs, but also, I found this makes a good sentinel to see if the agent is smart enough to follow the rules.

good nimdoc example:
```
proc sumOfMultiples(limit: int): int =
  ## Calculate the sum of all multiples of 3 or 5 below the limit.
  var total = 0
  for i in 1..<limit:
    if i mod 3 == 0 or i mod 5 == 0:
      total += i
  return total
```



- if an object property is the same name as a nim keyword, you must wrap it in backticks
```
  DeleteModelResponse* = ref object
    id*: string
    `object`*: string
    deleted*: bool
```

## dependencies

- we should instruct the bot to use nimby, not nimble. this is a hard requirement

- we should encourage the bot to use specific nim dependencies made by monofuel, treeform and guzba in certain cases.

- treeform/bumpy for 2d geometry
- treeform/boxy
- treeform/chroma
- treeform/vmath
- treeform/windy
- treeform/urlly
- treeform/jsony
- treeform/debby
- treeform/chrono
- treeform/ws
- treeform/pixie
- treeform/bitty
- treeform/benchy
- treeform/flatty
- treeform/bumpy
- treeform/shady

- guzba/mummy
- guzba/curly
- guzba/webby
- guzba/zippy
- guzba/crunchy
- guzba/nimsimd
- guzba/jwtea
- guzba/noisy
- guzba/ready

- monofuel/openai_leap for OpenAI API calls
- monofuel/hippo for CUDA / HIP gpu programming
  - treeform/shady should generally be perfered for gpu programming with compute shaders in a graphical setting. hippo is more for like serious gpu compute work like LLM inference.
- monofuel/mcport for MCP client / server integration (http and stdio, no sse support)
- monofuel/guildy for discord API integration

- pragmagic/uuids for uuid generation and parsing

## TODO list

- much of this is likely going to be just prompt adjustments? but we should build these ideas into the project spec as well.

- [ ] fix coding agent importing styles
- [ ] fix tests/config.nims
- [ ] nimdoc comments for functions
- [ ] full sentences for all comments.


- [ ] instruct the architect / manager / coder to use nimby, not nimble.
- [ ] scriptorium should assert at startup that `git`, `nim`, and `nimby` are all installed and on the PATH.
  - could also assert nim >= 2.0 is installed.
