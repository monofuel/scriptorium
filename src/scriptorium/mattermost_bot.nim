## Stub module for the Mattermost bot until ticket 0076 merges.

proc runMattermostBot*(repoPath: string) =
  ## Run the Mattermost bot event loop.
  echo "scriptorium: mattermost bot is not yet implemented (waiting for ticket 0076)"
  quit(1)
