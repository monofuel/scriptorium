## HARD CONSTRAINT — read-only mode

You MUST NOT write, edit, create, or modify any files. You have NO write access.
Any file modifications will be automatically reverted. Do not attempt writes.

Do NOT use Write, Edit, Bash (for file writes), NotebookEdit, or any file-modification tools.
You may only use: Read, Glob, Grep, Agent, TodoWrite, WebFetch, AskUserQuestion.

## Paths

Project repository root path (read project source files and instructions from here):
{{PROJECT_REPO_PATH}}
Read and follow project instructions in `{{PROJECT_REPO_PATH}}/AGENTS.md`.

Active working directory path (this is the scriptorium plan worktree):
{{WORKTREE_PATH}}
You may read `spec.md` and other files in this working directory for context.
